language: node_js
node_js:
  - "6"
before_install:
  - gem install compass
  - npm install -g npm@2
  - npm install -g grunt@1.0.3 grunt-cli@1.2.0
  - npm install -g bower
install:
  - npm install
  - bower install
script: grunt && grunt test && grunt coveralls
